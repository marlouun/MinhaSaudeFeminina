# Esta primeira versao nao depende de variaveis de ambiente nem backend.
# Quando uma API for adicionada, documente aqui apenas os nomes das variaveis.
# Nunca coloque credenciais reais neste arquivo.
# VITE_API_BASE_URL=https://api.exemplo.com
